export const IconAngleDoubleRight = (props: React.HTMLAttributes<never>) => {
  return (
    <svg {...props} data-testid={'icon-angle-double-right'}>
      {props.children}
    </svg>
  )
}

export const IconAngleRight = (props: React.HTMLAttributes<never>) => {
  return (
    <svg {...props} data-testid={'icon-angle-right'}>
      {props.children}
    </svg>
  )
}

export const IconCaretRight = (props: React.HTMLAttributes<never>) => {
  return (
    <svg {...props} data-testid={'icon-caret-right'}>
      {props.children}
    </svg>
  )
}

export const IconCheck = (props: React.HTMLAttributes<never>) => {
  return (
    <svg {...props} data-testid={'icon-check'}>
      {props.children}
    </svg>
  )
}

export const IconCircleDense = (props: React.HTMLAttributes<never>) => {
  return (
    <svg {...props} data-testid={'icon-circle-dense'}>
      {props.children}
    </svg>
  )
}

export const IconEllipsis = (props: React.HTMLAttributes<never>) => {
  return (
    <svg {...props} data-testid={'icon-ellipsis'}>
      {props.children}
    </svg>
  )
}
